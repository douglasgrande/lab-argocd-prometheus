apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: alertmanager-config-secret
  namespace: argocd
spec:
  project: default

  source:
    repoURL: 'https://github.com/douglasgrande/lab-argocd-prometheus.git'
    targetRevision: main
    path: app-values/alertmanager-config-secret

  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
      - ServerSideApply=true